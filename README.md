# ls-rust

## Install

```bash
git clone https://github.com/addereum/ls-rust.git
cd ls-rust
chmod +x install.sh
sudo ./install.sh
```

installs the binary to:

```
/usr/local/bin/ls
```

---

## Uninstall

```bash
chmod +x uninstall.sh
sudo ./uninstall.sh
```
